<h1>レビュー一覧</h1>

<% @reviews.each do |review| %>
  <div>
    <%= link_to review.id,
      artifact_review_path(
        review.artifact,
        review
      ) %>

    <%= review.result_label %>
  </div>
<% end %>