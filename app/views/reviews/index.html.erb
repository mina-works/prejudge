<h1><%= t(".title") %></h1>

<% @reviews.each do |review| %>
  <div>
    <%= link_to review.id,
      [review.artifact, review] %>

    <%= review.result_label %>
  </div>
<% end %>

<%= link_to t("common.back"),
  artifact_path(@artifact) %>